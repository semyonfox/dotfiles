# Device Fleet Private References

This directory is intentionally present but mostly untracked.

The `device-fleet` skill expects local reference files here, especially:

- `computers.md`
- `t3-code.md`

Those files contain private fleet inventory, access paths, LAN/Tailscale
addresses, service details, and operational history. Keep them local and out of
the public dotfiles repo.
