# Skill library

- `personal/` contains Semyon-specific workflows and constraints.
- `generic/` preserves installed/vendor material by source harness; do not rewrite it as personal guidance.
- Each `SKILL.md` keeps its source/deployment provenance in `metadata.harness`.
- Do not duplicate a package to make it visible to another harness; change deployment links only when compatibility is established.
