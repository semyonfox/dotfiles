# Auto-select a monitor for RustDesk/Wayland screen sharing.
# This avoids the peer-side XDPH picker prompt during unattended RustDesk sessions.
screencopy {
    allow_token_by_default = true
    custom_picker_binary = /home/semyon/.local/bin/hyprland-rustdesk-autopicker
}
