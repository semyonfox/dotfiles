# Server-only interactive shell overrides.
# Share update routing with Bash, including native Cursor and T3-managed Antigravity.

[[ -f "$HOME/.config/dotfiles/host.bash" ]] && source "$HOME/.config/dotfiles/host.bash"
