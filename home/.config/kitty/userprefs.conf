# Set your personal kitty configuration here
shell /usr/bin/zsh

# Enable true color (Kitty defaults to 256 color, this ensures full true color)
enable_true_color yes

# Terminfo for tmux compatibility
term xterm-256color
