# Copy this to ~/.cloudflared/access-tokens/ssh.semyon.ie.env on the machine
# that should bypass interactive Cloudflare Access login, then chmod 600 it.
CF_ACCESS_CLIENT_ID="REPLACE_WITH_CLIENT_ID"
CF_ACCESS_CLIENT_SECRET="REPLACE_WITH_CLIENT_SECRET"
